import React from 'react';

const Settings = () => {
  return (
    <div className="settings-page">
      <h2>Settings</h2>
      <p>Manage your account, preferences, and notifications here.</p>
      {/* Add settings controls as needed */}
    </div>
  );
};

export default Settings;
